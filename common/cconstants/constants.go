package cconstants

const (
	// ContentTypeApplicationJSON is the content type for
	// json requests/responses.
	ContentTypeApplicationJSON = "application/json"

	// UserInfoContextKey is the key which holds the user info
	// in the lambda context object.
	UserInfoContextKey = "UserInfo"
)
